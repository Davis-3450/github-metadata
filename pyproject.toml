[project]
name = "github-metadata"
version = "0.1.0"
description = "A Python package to retrieve metadata from your GitHub repositories."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["httpx>=0.28.1", "python-dotenv>=1.2.2"]
